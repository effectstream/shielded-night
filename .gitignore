node_modules/
dist/
reports/
midnight-level-db*/
.cache/
*.log

# Root .env holds SECRETS for the deploy/lock scripts (MN_MNEMONIC / MN_SEED;
# template in .env.example) — never commit it. frontend/.env is the opposite:
# committed on purpose, it holds only the public deployed contract addresses
# (audit trail in git history).
/.env
.env.local
.env.*.local

# Local tooling / state
.claude/
.wrangler/
.debug-tx.hex
