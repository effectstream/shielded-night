node_modules/
dist/
reports/
midnight-level-db*/
.cache/
*.log

# Local env overrides — never commit. frontend/.env itself IS committed: it
# holds only the public deployed contract addresses (audit trail in git
# history). Secrets stay in the shell env (MN_MNEMONIC/MN_SEED), never files.
.env.local
.env.*.local

# Local tooling / state
.claude/
.wrangler/
.debug-tx.hex
